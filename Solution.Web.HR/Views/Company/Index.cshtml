@using Solution.Core.Models.Common.Dto;
@model IEnumerable<CompanyDto>

@{
	var title = "Company List";
	ViewData["Title"] = title;
}
<partial name="_Notification" />
<div class="card">
	<div class="card-header">
		<div class="row">
			<div class="col-8">
				<h2>@title</h2>
			</div>
			<div class="col-4 text-end">
				<a asp-action="Create" class="btn btn-success text-white mt-1">Create New</a>
			</div>
		</div>
	</div>
	<div class="card-body">
		<table class="table table-striped table-bordered table-responsive">
			<thead class="bg-info">
				<tr>
					<th>@Html.DisplayNameFor(model => model.ComName)</th>
					<th>@Html.DisplayNameFor(model => model.Basic)</th>
					<th>@Html.DisplayNameFor(model => model.HRent)</th>
					<th>@Html.DisplayNameFor(model => model.Medical)</th>
					<th>@Html.DisplayNameFor(model => model.IsInactive)</th>
					<th>Action</th>
				</tr>
			</thead>
			<tbody>
				@foreach (var item in Model)
				{
					<tr>
						<td>@item.ComName</td>
						<td>@item.Basic</td>
						<td>@item.HRent</td>
						<td>@item.Medical</td>
						<td>
							<div class="form-check">
								<input class="form-check-input" type="checkbox" value="" asp-for="@item.IsInactive">
							</div>
						</td>
						<td>
							<a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-info">Edit</a>
							@*<a asp-action="Details" asp-route-id="@item.Id" class="btn btn-secondary">Details</a>*@
							<a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Delete</a>
						</td>
					</tr>
				}
			</tbody>
		</table>
	</div>
</div>